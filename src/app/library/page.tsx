import Link from "next/link";
import Image from "next/image";
import { ArrowUpRight, Gamepad2, Plus } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Surface } from "@/components/ui/surface";
import { games, patches } from "@/lib/mock-data";

export default function LibraryPage() { return <AppShell><div className="min-h-[calc(100vh-72px)] px-5 py-8 lg:px-9 lg:py-10"><div className="mx-auto max-w-[1240px]"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-[11px] font-bold uppercase tracking-[.18em] text-cyan">Workspace / library</p><h1 className="mt-3 text-3xl font-semibold tracking-tight text-white">My Library</h1><p className="mt-2 text-sm text-muted">12 games in your performance orbit.</p></div><Button variant="secondary"><Plus size={15} /> Add game</Button></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{games.map((game) => <Link href={`/games/${game.id}`} key={game.id}><Surface className="group overflow-hidden transition-all hover:-translate-y-0.5 hover:border-cyan/40"><div className="relative aspect-[1.4] overflow-hidden"><Image src={game.coverImageUrl} alt="" fill sizes="(max-width: 640px) 100vw, 25vw" className="object-cover transition-transform duration-500 group-hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-t from-black/80 via-black/10 to-transparent" /><Badge className="absolute left-3 top-3 border-white/15 bg-black/50 text-white">{game.status}</Badge><div className="absolute bottom-3 left-3"><p className="text-base font-semibold text-white">{game.title}</p><p className="mt-1 text-[11px] text-[#bbc4cb]">{game.genre}</p></div></div><div className="flex items-center justify-between p-4"><div><p className="text-[10px] uppercase tracking-[.13em] text-muted">Recent signals</p><p className="mt-1 text-xs font-semibold text-white">{patches.filter((patch) => patch.gameId === game.id).length} updates indexed</p></div><ArrowUpRight size={16} className="text-cyan" /></div></Surface></Link>)}<Surface className="grid min-h-[270px] place-items-center border-dashed bg-transparent p-5"><div className="text-center"><div className="mx-auto grid size-11 place-items-center rounded-xl bg-white/[.04] text-muted"><Gamepad2 size={20} /></div><p className="mt-4 text-sm font-semibold text-white">Expand your radar</p><p className="mt-1 max-w-[180px] text-xs leading-5 text-muted">Add another game to start receiving patch intelligence.</p><button className="mt-4 text-xs font-semibold text-cyan">Browse Steam library</button></div></Surface></div></div></div></AppShell>; }
